{
  "name": "defiance",
  "version": "1.0.0",
  "description": "driver tracking server",
  "main": "server.js",
  "author": "F&R TEC",
  "license": "ISC",
  "scripts": {
    "start": "node server.js",
    "dev": "set NODE_ENV=dev&& node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "test": "set NODE_ENV=test&& mocha --recursive --parallel",
    "testLocal": "set NODE_ENV=test_local&& mocha --recursive --parallel --watch",
    "coverage": "nyc report --reporter=html"
  },
  "engines": {
    "node": "12.18.2"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "config": "^3.3.1",
    "create-react-app": "^4.0.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-async-errors": "^3.1.1",
    "helmet": "^4.1.1",
    "joi": "^17.2.1",
    "joi-objectid": "^3.0.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.9.27",
    "morgan": "^1.10.0",
    "socket.io": "^2.3.0",
    "winston": "^3.3.3",
    "winston-mongodb": "^5.0.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "mocha": "^8.2.1",
    "nyc": "^15.1.0"
  }
}
